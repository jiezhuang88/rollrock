<%= form_for(@admin_menu) :html => { :multipart => true } do |f| %>
  <% if @admin_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_menu.errors.count, "error") %> prohibited this admin_menu from being saved:</h2>

      <ul>
      <% @admin_menu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br />
    <%= f.text_field :parent_id %>
  </div>
  <%= f.file_field :face %>
  <div class="field">
    <%= f.label :isleaf %><br />
    <%= f.check_box :isleaf %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
